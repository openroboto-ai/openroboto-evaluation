repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.21  # keep in sync with the ruff version used locally
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format
